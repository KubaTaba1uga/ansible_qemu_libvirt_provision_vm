prometheus_servers:
  hosts:
    prometheus.lab:

prometheus_agents:
  hosts:
    emacs.lab:
    forgejo.lab:
    mindmade.lab:

emacs_servers:
  hosts:
    emacs.lab:

forgejo_servers:
  hosts:
    forgejo.lab:
